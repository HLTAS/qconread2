sudo: required
dist: trusty

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq qtbase5-dev

compiler:
  - gcc
  - clang

script:
  - mkdir build
  - cd build
  - cmake ..
  - make -j
